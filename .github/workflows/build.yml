name: Build and Sync Admin App

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # علشان يقدر يرفع ملفات للمستودع

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Generate Platform Folders
        run: flutter create . --platforms android

      # --- الخطوة السحرية: إرجاع الملفات للمستودع ---
      - name: Push Generated Files back to Repo
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "GitHub Actions: Generated Android platform files" || echo "No changes to commit"
          git push
      # --------------------------------------------

      - name: Install Dependencies
        run: flutter pub get

      - name: Build Android APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MasterStore-Admin-APK
          path: build/app/outputs/flutter-apk/app-release.apk
